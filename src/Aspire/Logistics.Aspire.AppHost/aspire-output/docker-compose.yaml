services:
  compose-dashboard:
    image: "mcr.microsoft.com/dotnet/nightly/aspire-dashboard:latest"
    ports:
      - "7100:18888"
    expose:
      - "18889"
      - "18890"
    networks:
      - "aspire"
    restart: "always"
  identity-server:
    image: "${IDENTITY_SERVER_IMAGE}"
    environment:
      OTEL_DOTNET_EXPERIMENTAL_OTLP_EMIT_EXCEPTION_LOG_ATTRIBUTES: "true"
      OTEL_DOTNET_EXPERIMENTAL_OTLP_EMIT_EVENT_LOG_ATTRIBUTES: "true"
      OTEL_DOTNET_EXPERIMENTAL_OTLP_RETRY: "in_memory"
      ASPNETCORE_FORWARDEDHEADERS_ENABLED: "true"
      HTTP_PORTS: "${IDENTITY_SERVER_PORT}"
      ConnectionStrings__MasterDatabase: "${ConnectionStrings__MasterDatabase}"
      ConnectionStrings__DefaultTenantDatabase: "${ConnectionStrings__DefaultTenantDatabase}"
      Authentication__Google__ClientId: "${Authentication__Google__ClientId}"
      Authentication__Google__ClientSecret: "${Authentication__Google__ClientSecret}"
      GoogleRecaptcha__SecretKey: "${GoogleRecaptcha__SecretKey}"
      GoogleRecaptcha__SiteKey: "${GoogleRecaptcha__SiteKey}"
      Impersonation__TmsPortalUrl: "${Impersonation__TmsPortalUrl}"
      Resend__ApiKey: "${Resend__ApiKey}"
      Resend__SenderEmail: "${Resend__SenderEmail}"
      Resend__SenderName: "${Resend__SenderName}"
      OTEL_EXPORTER_OTLP_ENDPOINT: "http://compose-dashboard:18889"
      OTEL_EXPORTER_OTLP_PROTOCOL: "grpc"
      OTEL_SERVICE_NAME: "identity-server"
    ports:
      - "${IDENTITY_SERVER_PORT}:${IDENTITY_SERVER_PORT}"
    networks:
      - "aspire"
  api:
    image: "${API_IMAGE}"
    environment:
      OTEL_DOTNET_EXPERIMENTAL_OTLP_EMIT_EXCEPTION_LOG_ATTRIBUTES: "true"
      OTEL_DOTNET_EXPERIMENTAL_OTLP_EMIT_EVENT_LOG_ATTRIBUTES: "true"
      OTEL_DOTNET_EXPERIMENTAL_OTLP_RETRY: "in_memory"
      ASPNETCORE_FORWARDEDHEADERS_ENABLED: "true"
      HTTP_PORTS: "${API_PORT}"
      ConnectionStrings__MasterDatabase: "${ConnectionStrings__MasterDatabase}"
      ConnectionStrings__DefaultTenantDatabase: "${ConnectionStrings__DefaultTenantDatabase}"
      IdentityServer__Authority: "http://identity-server:7001"
      IdentityServer__ExternalAuthority: "${IdentityServer__ExternalAuthority}"
      IdentityServer__RequireHttpsMetadata: "false"
      Impersonation__MasterPassword: "${Impersonation__MasterPassword}"
      Resend__ApiKey: "${Resend__ApiKey}"
      Resend__SenderEmail: "${Resend__SenderEmail}"
      Resend__SenderName: "${Resend__SenderName}"
      Stripe__SecretKey: "${Stripe__SecretKey}"
      Stripe__PublishableKey: "${Stripe__PublishableKey}"
      Stripe__WebhookSecret: "${Stripe__WebhookSecret}"
      Mapbox__AccessToken: "${Mapbox__AccessToken}"
      CustomerPortal__BaseUrl: "${CustomerPortal__BaseUrl}"
      FileBlobStorage__BaseUrl: "${FileBlobStorage__BaseUrl}"
      TenantsDatabaseConfig__DatabaseNameTemplate: "${TenantsDatabaseConfig__DatabaseNameTemplate}"
      TenantsDatabaseConfig__DatabaseHost: "${TenantsDatabaseConfig__DatabaseHost}"
      TenantsDatabaseConfig__DatabasePort: "${TenantsDatabaseConfig__DatabasePort}"
      TenantsDatabaseConfig__DatabaseUserId: "${TenantsDatabaseConfig__DatabaseUserId}"
      TenantsDatabaseConfig__DatabasePassword: "${TenantsDatabaseConfig__DatabasePassword}"
      OTEL_EXPORTER_OTLP_ENDPOINT: "http://compose-dashboard:18889"
      OTEL_EXPORTER_OTLP_PROTOCOL: "grpc"
      OTEL_SERVICE_NAME: "api"
    ports:
      - "${API_PORT}:${API_PORT}"
    depends_on:
      identity-server:
        condition: "service_started"
    networks:
      - "aspire"
  admin-portal:
    image: "ghcr.io/suxrobgm/logistics-app/admin-portal:latest"
    ports:
      - "7002:80"
    depends_on:
      api:
        condition: "service_started"
      identity-server:
        condition: "service_started"
    networks:
      - "aspire"
  tms-portal:
    image: "ghcr.io/suxrobgm/logistics-app/tms-portal:latest"
    environment:
      MAPBOX_TOKEN: "${Mapbox__AccessToken}"
      STRIPE_PUBLISHABLE_KEY: "${Stripe__PublishableKey}"
    ports:
      - "7003:80"
    depends_on:
      api:
        condition: "service_started"
      identity-server:
        condition: "service_started"
    networks:
      - "aspire"
  customer-portal:
    image: "ghcr.io/suxrobgm/logistics-app/customer-portal:latest"
    ports:
      - "7004:80"
    depends_on:
      api:
        condition: "service_started"
      identity-server:
        condition: "service_started"
    networks:
      - "aspire"
  website:
    image: "ghcr.io/suxrobgm/logistics-app/website:latest"
    ports:
      - "7005:7005"
    depends_on:
      api:
        condition: "service_started"
    networks:
      - "aspire"
  portainer-agent:
    image: "portainer/agent:latest"
    expose:
      - "9001"
    volumes:
      - type: "volume"
        target: "/var/lib/docker/volumes"
        source: "portainer-agent-data"
        read_only: false
      - type: "bind"
        target: "/var/run/docker.sock"
        source: "/var/run/docker.sock"
        read_only: true
    networks:
      - "aspire"
  portainer:
    image: "portainer/portainer-ce:latest"
    ports:
      - "9000:9000"
    volumes:
      - type: "volume"
        target: "/data"
        source: "portainer-data"
        read_only: false
      - type: "bind"
        target: "/var/run/docker.sock"
        source: "/var/run/docker.sock"
        read_only: true
    depends_on:
      portainer-agent:
        condition: "service_started"
    networks:
      - "aspire"
networks:
  aspire:
    driver: "bridge"
volumes:
  portainer-agent-data:
    driver: "local"
  portainer-data:
    driver: "local"
