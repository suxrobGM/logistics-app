namespace Logistics.Application.Modules.Common.Constants;

public static class PlatformConstants
{
    /// <summary>
    /// Display name of the platform, used where there is no tenant company name to fall back on
    /// (e.g. app-level admin invitations and notifications).
    /// </summary>
    public const string PlatformName = "LogisticsX";
}
