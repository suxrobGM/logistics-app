export enum TripStatus {
  Planned = "planned",
  Dispatched = "dispatched",
  InTransit = "in_transit",
  Cancelled = "cancelled",
  Completed = "completed",
}

export enum TripStopType {
  PickUp = "pick_up",
  DropOff = "drop_off",
}
