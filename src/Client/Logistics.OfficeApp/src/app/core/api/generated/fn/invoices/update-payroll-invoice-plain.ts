/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { Result } from '../../models/result';
import type { UpdatePayrollInvoiceCommand } from '../../models/update-payroll-invoice-command';

export interface UpdatePayrollInvoice$Plain$Params {
  id: string;
      body?: UpdatePayrollInvoiceCommand
}

export function updatePayrollInvoice$Plain(http: HttpClient, rootUrl: string, params: UpdatePayrollInvoice$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<Result>> {
  const rb = new RequestBuilder(rootUrl, updatePayrollInvoice$Plain.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Result>;
    })
  );
}

updatePayrollInvoice$Plain.PATH = '/invoices/payrolls/{id}';
