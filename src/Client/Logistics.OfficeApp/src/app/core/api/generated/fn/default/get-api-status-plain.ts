/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { StringResult } from '../../models/string-result';

export interface GetApiStatus$Plain$Params {
}

export function getApiStatus$Plain(http: HttpClient, rootUrl: string, params?: GetApiStatus$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<StringResult>> {
  const rb = new RequestBuilder(rootUrl, getApiStatus$Plain.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<StringResult>;
    })
  );
}

getApiStatus$Plain.PATH = '/';
