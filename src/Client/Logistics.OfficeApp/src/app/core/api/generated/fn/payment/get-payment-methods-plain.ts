/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { PaymentMethodDto } from '../../models/payment-method-dto';

export interface GetPaymentMethods$Plain$Params {
  OrderBy?: string;
}

export function getPaymentMethods$Plain(http: HttpClient, rootUrl: string, params?: GetPaymentMethods$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<PaymentMethodDto>>> {
  const rb = new RequestBuilder(rootUrl, getPaymentMethods$Plain.PATH, 'get');
  if (params) {
    rb.query('OrderBy', params.OrderBy, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<PaymentMethodDto>>;
    })
  );
}

getPaymentMethods$Plain.PATH = '/payments/methods';
