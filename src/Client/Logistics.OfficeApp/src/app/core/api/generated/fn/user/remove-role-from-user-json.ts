/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RemoveRoleFromUserCommand } from '../../models/remove-role-from-user-command';
import { Result } from '../../models/result';

export interface RemoveRoleFromUser$Json$Params {
  id: string;
      body?: RemoveRoleFromUserCommand
}

export function removeRoleFromUser$Json(http: HttpClient, rootUrl: string, params: RemoveRoleFromUser$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<Result>> {
  const rb = new RequestBuilder(rootUrl, removeRoleFromUser$Json.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Result>;
    })
  );
}

removeRoleFromUser$Json.PATH = '/users/{id}/remove-role';
