/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { ConversationDto } from '../../models/conversation-dto';

export interface GetConversations$Params {
  ParticipantId?: string;
  LoadId?: string;
}

export function getConversations(http: HttpClient, rootUrl: string, params?: GetConversations$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<ConversationDto>>> {
  const rb = new RequestBuilder(rootUrl, getConversations.PATH, 'get');
  if (params) {
    rb.query('ParticipantId', params.ParticipantId, {});
    rb.query('LoadId', params.LoadId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<ConversationDto>>;
    })
  );
}

getConversations.PATH = '/messages/conversations';
