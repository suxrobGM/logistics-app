/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { PortalLoadDtoPagedResponse } from '../../models/portal-load-dto-paged-response';

export interface GetPortalLoads$Params {
  Search?: string;
  Page?: number;
  PageSize?: number;
  OrderBy?: string;
  OnlyActiveLoads?: boolean;
  StartDate?: string;
  EndDate?: string;
}

export function getPortalLoads(http: HttpClient, rootUrl: string, params?: GetPortalLoads$Params, context?: HttpContext): Observable<StrictHttpResponse<PortalLoadDtoPagedResponse>> {
  const rb = new RequestBuilder(rootUrl, getPortalLoads.PATH, 'get');
  if (params) {
    rb.query('Search', params.Search, {});
    rb.query('Page', params.Page, {});
    rb.query('PageSize', params.PageSize, {});
    rb.query('OrderBy', params.OrderBy, {});
    rb.query('OnlyActiveLoads', params.OnlyActiveLoads, {});
    rb.query('StartDate', params.StartDate, {});
    rb.query('EndDate', params.EndDate, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PortalLoadDtoPagedResponse>;
    })
  );
}

getPortalLoads.PATH = '/portal/loads';
