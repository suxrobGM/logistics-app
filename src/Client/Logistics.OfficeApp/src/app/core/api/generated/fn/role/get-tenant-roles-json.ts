/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RoleDtoPagedResult } from '../../models/role-dto-paged-result';

export interface GetTenantRoles$Json$Params {
  Search?: string;
  OrderBy?: string;
  Page?: number;
  PageSize?: number;
}

export function getTenantRoles$Json(http: HttpClient, rootUrl: string, params?: GetTenantRoles$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<RoleDtoPagedResult>> {
  const rb = new RequestBuilder(rootUrl, getTenantRoles$Json.PATH, 'get');
  if (params) {
    rb.query('Search', params.Search, {});
    rb.query('OrderBy', params.OrderBy, {});
    rb.query('Page', params.Page, {});
    rb.query('PageSize', params.PageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RoleDtoPagedResult>;
    })
  );
}

getTenantRoles$Json.PATH = '/roles/tenant';
