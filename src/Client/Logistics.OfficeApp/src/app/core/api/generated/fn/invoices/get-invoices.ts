/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { InvoiceDtoPagedResponse } from '../../models/invoice-dto-paged-response';
import type { InvoiceType } from '../../models/invoice-type';

export interface GetInvoices$Params {
  LoadId?: string;
  EmployeeId?: string;
  EmployeeName?: string;
  InvoiceType?: InvoiceType;
  OrderBy?: string;
  Page?: number;
  PageSize?: number;
}

export function getInvoices(http: HttpClient, rootUrl: string, params?: GetInvoices$Params, context?: HttpContext): Observable<StrictHttpResponse<InvoiceDtoPagedResponse>> {
  const rb = new RequestBuilder(rootUrl, getInvoices.PATH, 'get');
  if (params) {
    rb.query('LoadId', params.LoadId, {});
    rb.query('EmployeeId', params.EmployeeId, {});
    rb.query('EmployeeName', params.EmployeeName, {});
    rb.query('InvoiceType', params.InvoiceType, {});
    rb.query('OrderBy', params.OrderBy, {});
    rb.query('Page', params.Page, {});
    rb.query('PageSize', params.PageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<InvoiceDtoPagedResponse>;
    })
  );
}

getInvoices.PATH = '/invoices';
