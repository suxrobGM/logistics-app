/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { Result } from '../../models/result';
import type { SetDriverDeviceTokenCommand } from '../../models/set-driver-device-token-command';

export interface SetDriverDeviceToken$Plain$Params {
  userId: string;
      body?: SetDriverDeviceTokenCommand
}

export function setDriverDeviceToken$Plain(http: HttpClient, rootUrl: string, params: SetDriverDeviceToken$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<Result>> {
  const rb = new RequestBuilder(rootUrl, setDriverDeviceToken$Plain.PATH, 'post');
  if (params) {
    rb.path('userId', params.userId, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Result>;
    })
  );
}

setDriverDeviceToken$Plain.PATH = '/drivers/{userId}/device-token';
