/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { CompanyStatsDto } from '../../models/company-stats-dto';
import type { GetCompanyStatsQuery } from '../../models/get-company-stats-query';

export interface GetCompanyStats$Plain$Params {
  request?: GetCompanyStatsQuery;
}

export function getCompanyStats$Plain(http: HttpClient, rootUrl: string, params?: GetCompanyStats$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<CompanyStatsDto>> {
  const rb = new RequestBuilder(rootUrl, getCompanyStats$Plain.PATH, 'get');
  if (params) {
    rb.query('request', params.request, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CompanyStatsDto>;
    })
  );
}

getCompanyStats$Plain.PATH = '/stats/company';
