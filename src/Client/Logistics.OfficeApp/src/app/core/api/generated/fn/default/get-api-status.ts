/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { StringResult } from '../../models/string-result';

export interface GetApiStatus$Params {
}

export function getApiStatus(http: HttpClient, rootUrl: string, params?: GetApiStatus$Params, context?: HttpContext): Observable<StrictHttpResponse<StringResult>> {
  const rb = new RequestBuilder(rootUrl, getApiStatus.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<StringResult>;
    })
  );
}

getApiStatus.PATH = '/';
