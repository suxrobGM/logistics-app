/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { CustomerUserDto } from '../../models/customer-user-dto';

export interface GetPortalProfile$Params {
}

export function getPortalProfile(http: HttpClient, rootUrl: string, params?: GetPortalProfile$Params, context?: HttpContext): Observable<StrictHttpResponse<CustomerUserDto>> {
  const rb = new RequestBuilder(rootUrl, getPortalProfile.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CustomerUserDto>;
    })
  );
}

getPortalProfile.PATH = '/portal/me';
