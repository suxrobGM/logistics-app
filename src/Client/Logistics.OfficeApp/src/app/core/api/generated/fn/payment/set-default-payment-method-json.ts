/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Result } from '../../models/result';
import { SetDefaultPaymentMethodCommand } from '../../models/set-default-payment-method-command';

export interface SetDefaultPaymentMethod$Json$Params {
      body?: SetDefaultPaymentMethodCommand
}

export function setDefaultPaymentMethod$Json(http: HttpClient, rootUrl: string, params?: SetDefaultPaymentMethod$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<Result>> {
  const rb = new RequestBuilder(rootUrl, setDefaultPaymentMethod$Json.PATH, 'put');
  if (params) {
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Result>;
    })
  );
}

setDefaultPaymentMethod$Json.PATH = '/payments/methods/default';
