/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DriverDashboardDtoResult } from '../../models/driver-dashboard-dto-result';

export interface GetDriverDashboard$Plain$Params {
  StartDate?: string;
  EndDate?: string;
  OrderBy?: string;
  Page?: number;
  PageSize?: number;
}

export function getDriverDashboard$Plain(http: HttpClient, rootUrl: string, params?: GetDriverDashboard$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<DriverDashboardDtoResult>> {
  const rb = new RequestBuilder(rootUrl, getDriverDashboard$Plain.PATH, 'get');
  if (params) {
    rb.query('StartDate', params.StartDate, {});
    rb.query('EndDate', params.EndDate, {});
    rb.query('OrderBy', params.OrderBy, {});
    rb.query('Page', params.Page, {});
    rb.query('PageSize', params.PageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DriverDashboardDtoResult>;
    })
  );
}

getDriverDashboard$Plain.PATH = '/reports/drivers/dashboard';
