/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { DecodeVinRequest } from '../../models/decode-vin-request';
import type { VehicleInfoDto } from '../../models/vehicle-info-dto';

export interface DecodeVin$Params {
      body?: DecodeVinRequest
}

export function decodeVin(http: HttpClient, rootUrl: string, params?: DecodeVin$Params, context?: HttpContext): Observable<StrictHttpResponse<VehicleInfoDto>> {
  const rb = new RequestBuilder(rootUrl, decodeVin.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<VehicleInfoDto>;
    })
  );
}

decodeVin.PATH = '/inspections/decode-vin';
