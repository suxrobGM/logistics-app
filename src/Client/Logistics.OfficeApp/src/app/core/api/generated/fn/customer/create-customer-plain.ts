/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { CreateCustomerCommand } from '../../models/create-customer-command';
import type { CustomerDtoResult } from '../../models/customer-dto-result';

export interface CreateCustomer$Plain$Params {
      body?: CreateCustomerCommand
}

export function createCustomer$Plain(http: HttpClient, rootUrl: string, params?: CreateCustomer$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<CustomerDtoResult>> {
  const rb = new RequestBuilder(rootUrl, createCustomer$Plain.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CustomerDtoResult>;
    })
  );
}

createCustomer$Plain.PATH = '/customers';
