/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { EldDriverDto } from '../../models/eld-driver-dto';

export interface GetEldProviderDrivers$Params {
  providerId: string;
}

export function getEldProviderDrivers(http: HttpClient, rootUrl: string, params: GetEldProviderDrivers$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<EldDriverDto>>> {
  const rb = new RequestBuilder(rootUrl, getEldProviderDrivers.PATH, 'get');
  if (params) {
    rb.path('providerId', params.providerId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<EldDriverDto>>;
    })
  );
}

getEldProviderDrivers.PATH = '/eld/providers/{providerId}/drivers';
