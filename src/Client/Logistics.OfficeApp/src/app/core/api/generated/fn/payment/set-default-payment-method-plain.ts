/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Result } from '../../models/result';
import { SetDefaultPaymentMethodCommand } from '../../models/set-default-payment-method-command';

export interface SetDefaultPaymentMethod$Plain$Params {
      body?: SetDefaultPaymentMethodCommand
}

export function setDefaultPaymentMethod$Plain(http: HttpClient, rootUrl: string, params?: SetDefaultPaymentMethod$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<Result>> {
  const rb = new RequestBuilder(rootUrl, setDefaultPaymentMethod$Plain.PATH, 'put');
  if (params) {
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Result>;
    })
  );
}

setDefaultPaymentMethod$Plain.PATH = '/payments/methods/default';
