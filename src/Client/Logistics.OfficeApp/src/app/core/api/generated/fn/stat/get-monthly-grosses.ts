/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { MonthlyGrossesDto } from '../../models/monthly-grosses-dto';

export interface GetMonthlyGrosses$Params {
  TruckId?: string;
  UserId?: string;
  StartDate?: string;
  EndDate?: string;
}

export function getMonthlyGrosses(http: HttpClient, rootUrl: string, params?: GetMonthlyGrosses$Params, context?: HttpContext): Observable<StrictHttpResponse<MonthlyGrossesDto>> {
  const rb = new RequestBuilder(rootUrl, getMonthlyGrosses.PATH, 'get');
  if (params) {
    rb.query('TruckId', params.TruckId, {});
    rb.query('UserId', params.UserId, {});
    rb.query('StartDate', params.StartDate, {});
    rb.query('EndDate', params.EndDate, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<MonthlyGrossesDto>;
    })
  );
}

getMonthlyGrosses.PATH = '/stats/monthly-grosses';
