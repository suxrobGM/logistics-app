/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { CustomerUserDto } from '../../models/customer-user-dto';

export interface GetCustomerUserById$Params {
  id: string;
}

export function getCustomerUserById(http: HttpClient, rootUrl: string, params: GetCustomerUserById$Params, context?: HttpContext): Observable<StrictHttpResponse<CustomerUserDto>> {
  const rb = new RequestBuilder(rootUrl, getCustomerUserById.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CustomerUserDto>;
    })
  );
}

getCustomerUserById.PATH = '/customer-users/{id}';
