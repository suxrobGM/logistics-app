/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { PortalLoadDto } from '../../models/portal-load-dto';

export interface GetPortalLoad$Params {
  loadId: string;
}

export function getPortalLoad(http: HttpClient, rootUrl: string, params: GetPortalLoad$Params, context?: HttpContext): Observable<StrictHttpResponse<PortalLoadDto>> {
  const rb = new RequestBuilder(rootUrl, getPortalLoad.PATH, 'get');
  if (params) {
    rb.path('loadId', params.loadId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PortalLoadDto>;
    })
  );
}

getPortalLoad.PATH = '/portal/loads/{loadId}';
