/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { NotificationDtoArrayResult } from '../../models/notification-dto-array-result';

export interface GetNotifications$Json$Params {
  StartDate?: string;
  EndDate?: string;
}

export function getNotifications$Json(http: HttpClient, rootUrl: string, params?: GetNotifications$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<NotificationDtoArrayResult>> {
  const rb = new RequestBuilder(rootUrl, getNotifications$Json.PATH, 'get');
  if (params) {
    rb.query('StartDate', params.StartDate, {});
    rb.query('EndDate', params.EndDate, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<NotificationDtoArrayResult>;
    })
  );
}

getNotifications$Json.PATH = '/notifications';
