/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { DriverStatsDto } from '../../models/driver-stats-dto';

export interface GetDriverStats$Params {
  userId: string;
  UserId?: string;
}

export function getDriverStats(http: HttpClient, rootUrl: string, params: GetDriverStats$Params, context?: HttpContext): Observable<StrictHttpResponse<DriverStatsDto>> {
  const rb = new RequestBuilder(rootUrl, getDriverStats.PATH, 'get');
  if (params) {
    rb.path('userId', params.userId, {});
    rb.query('UserId', params.UserId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DriverStatsDto>;
    })
  );
}

getDriverStats.PATH = '/stats/driver/{userId}';
