/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { AccidentReportDto } from '../../models/accident-report-dto';
import type { ResolveAccidentReportRequest } from '../../models/resolve-accident-report-request';

export interface ResolveAccidentReport$Params {
  id: string;
      body?: ResolveAccidentReportRequest
}

export function resolveAccidentReport(http: HttpClient, rootUrl: string, params: ResolveAccidentReport$Params, context?: HttpContext): Observable<StrictHttpResponse<AccidentReportDto>> {
  const rb = new RequestBuilder(rootUrl, resolveAccidentReport.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<AccidentReportDto>;
    })
  );
}

resolveAccidentReport.PATH = '/safety/accidents/{id}/resolve';
