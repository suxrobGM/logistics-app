/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { ResolveExceptionRequest } from '../../models/resolve-exception-request';

export interface ResolveLoadException$Params {
  id: string;
  exceptionId: string;
      body?: ResolveExceptionRequest
}

export function resolveLoadException(http: HttpClient, rootUrl: string, params: ResolveLoadException$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, resolveLoadException.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.path('exceptionId', params.exceptionId, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

resolveLoadException.PATH = '/loads/{id}/exceptions/{exceptionId}/resolve';
