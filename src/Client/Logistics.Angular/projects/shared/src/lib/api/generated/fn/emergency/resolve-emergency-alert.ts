/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { EmergencyAlertDto } from '../../models/emergency-alert-dto';
import type { ResolveEmergencyAlertRequest } from '../../models/resolve-emergency-alert-request';

export interface ResolveEmergencyAlert$Params {
  id: string;
      body?: ResolveEmergencyAlertRequest
}

export function resolveEmergencyAlert(http: HttpClient, rootUrl: string, params: ResolveEmergencyAlert$Params, context?: HttpContext): Observable<StrictHttpResponse<EmergencyAlertDto>> {
  const rb = new RequestBuilder(rootUrl, resolveEmergencyAlert.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<EmergencyAlertDto>;
    })
  );
}

resolveEmergencyAlert.PATH = '/safety/emergency/alerts/{id}/resolve';
