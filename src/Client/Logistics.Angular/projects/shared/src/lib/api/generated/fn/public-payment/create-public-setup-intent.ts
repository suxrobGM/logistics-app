/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { SetupIntentDto } from '../../models/setup-intent-dto';

export interface CreatePublicSetupIntent$Params {
  tenantId: string;
  token: string;
}

export function createPublicSetupIntent(http: HttpClient, rootUrl: string, params: CreatePublicSetupIntent$Params, context?: HttpContext): Observable<StrictHttpResponse<SetupIntentDto>> {
  const rb = new RequestBuilder(rootUrl, createPublicSetupIntent.PATH, 'post');
  if (params) {
    rb.path('tenantId', params.tenantId, {});
    rb.path('token', params.token, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SetupIntentDto>;
    })
  );
}

createPublicSetupIntent.PATH = '/public/payments/{tenantId}/{token}/setup';
