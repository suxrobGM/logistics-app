/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { LoadBoardSearchResultDto } from '../../models/load-board-search-result-dto';
import type { SearchLoadBoardCommand } from '../../models/search-load-board-command';

export interface SearchLoadBoard$Params {
      body?: SearchLoadBoardCommand
}

export function searchLoadBoard(http: HttpClient, rootUrl: string, params?: SearchLoadBoard$Params, context?: HttpContext): Observable<StrictHttpResponse<LoadBoardSearchResultDto>> {
  const rb = new RequestBuilder(rootUrl, searchLoadBoard.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<LoadBoardSearchResultDto>;
    })
  );
}

searchLoadBoard.PATH = '/loadboard/search';
