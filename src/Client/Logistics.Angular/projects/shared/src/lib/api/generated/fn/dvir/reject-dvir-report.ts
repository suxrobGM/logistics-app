/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { DvirReportDto } from '../../models/dvir-report-dto';
import type { RejectDvirReportRequest } from '../../models/reject-dvir-report-request';

export interface RejectDvirReport$Params {
  id: string;
      body?: RejectDvirReportRequest
}

export function rejectDvirReport(http: HttpClient, rootUrl: string, params: RejectDvirReport$Params, context?: HttpContext): Observable<StrictHttpResponse<DvirReportDto>> {
  const rb = new RequestBuilder(rootUrl, rejectDvirReport.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DvirReportDto>;
    })
  );
}

rejectDvirReport.PATH = '/dvir/{id}/reject';
