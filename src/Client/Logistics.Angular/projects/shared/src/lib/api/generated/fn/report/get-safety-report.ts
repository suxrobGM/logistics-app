/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { SafetyReportDto } from '../../models/safety-report-dto';

export interface GetSafetyReport$Params {
  StartDate?: string;
  EndDate?: string;
  OrderBy?: string;
  Page?: number;
  PageSize?: number;
}

export function getSafetyReport(http: HttpClient, rootUrl: string, params?: GetSafetyReport$Params, context?: HttpContext): Observable<StrictHttpResponse<SafetyReportDto>> {
  const rb = new RequestBuilder(rootUrl, getSafetyReport.PATH, 'get');
  if (params) {
    rb.query('StartDate', params.StartDate, {});
    rb.query('EndDate', params.EndDate, {});
    rb.query('OrderBy', params.OrderBy, {});
    rb.query('Page', params.Page, {});
    rb.query('PageSize', params.PageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SafetyReportDto>;
    })
  );
}

getSafetyReport.PATH = '/reports/safety';
