/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { LoadDtoPagedResponse } from '../../models/load-dto-paged-response';
import type { LoadStatus } from '../../models/load-status';
import type { LoadType } from '../../models/load-type';

export interface GetLoads$Params {
  LoadAllPages?: boolean;
  OnlyActiveLoads?: boolean;
  UserId?: string;
  TruckId?: string;
  StartDate?: string;
  EndDate?: string;
  Statuses?: Array<LoadStatus>;
  Types?: Array<LoadType>;
  CustomerId?: string;
  Search?: string;
  OrderBy?: string;
  Page?: number;
  PageSize?: number;
}

export function getLoads(http: HttpClient, rootUrl: string, params?: GetLoads$Params, context?: HttpContext): Observable<StrictHttpResponse<LoadDtoPagedResponse>> {
  const rb = new RequestBuilder(rootUrl, getLoads.PATH, 'get');
  if (params) {
    rb.query('LoadAllPages', params.LoadAllPages, {});
    rb.query('OnlyActiveLoads', params.OnlyActiveLoads, {});
    rb.query('UserId', params.UserId, {});
    rb.query('TruckId', params.TruckId, {});
    rb.query('StartDate', params.StartDate, {});
    rb.query('EndDate', params.EndDate, {});
    rb.query('Statuses', params.Statuses, {});
    rb.query('Types', params.Types, {});
    rb.query('CustomerId', params.CustomerId, {});
    rb.query('Search', params.Search, {});
    rb.query('OrderBy', params.OrderBy, {});
    rb.query('Page', params.Page, {});
    rb.query('PageSize', params.PageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<LoadDtoPagedResponse>;
    })
  );
}

getLoads.PATH = '/loads';
