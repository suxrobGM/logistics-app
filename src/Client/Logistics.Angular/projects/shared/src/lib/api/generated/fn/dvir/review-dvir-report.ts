/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { DvirReportDto } from '../../models/dvir-report-dto';
import type { ReviewDvirReportCommand } from '../../models/review-dvir-report-command';

export interface ReviewDvirReport$Params {
  id: string;
      body?: ReviewDvirReportCommand
}

export function reviewDvirReport(http: HttpClient, rootUrl: string, params: ReviewDvirReport$Params, context?: HttpContext): Observable<StrictHttpResponse<DvirReportDto>> {
  const rb = new RequestBuilder(rootUrl, reviewDvirReport.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DvirReportDto>;
    })
  );
}

reviewDvirReport.PATH = '/dvir/{id}/review';
