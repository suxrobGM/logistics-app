/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { AccidentReportDto } from '../../models/accident-report-dto';

export interface SubmitAccidentReport$Params {
  id: string;
}

export function submitAccidentReport(http: HttpClient, rootUrl: string, params: SubmitAccidentReport$Params, context?: HttpContext): Observable<StrictHttpResponse<AccidentReportDto>> {
  const rb = new RequestBuilder(rootUrl, submitAccidentReport.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<AccidentReportDto>;
    })
  );
}

submitAccidentReport.PATH = '/safety/accidents/{id}/submit';
