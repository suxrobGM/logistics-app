/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { DriverBehaviorEventDtoPagedResponse } from '../../models/driver-behavior-event-dto-paged-response';
import type { DriverBehaviorEventType } from '../../models/driver-behavior-event-type';

export interface GetDriverBehaviorEvents$Params {
  DriverId?: string;
  TruckId?: string;
  EventType?: DriverBehaviorEventType;
  IsReviewed?: boolean;
  FromDate?: string;
  ToDate?: string;
  Search?: string;
  OrderBy?: string;
  Page?: number;
  PageSize?: number;
}

export function getDriverBehaviorEvents(http: HttpClient, rootUrl: string, params?: GetDriverBehaviorEvents$Params, context?: HttpContext): Observable<StrictHttpResponse<DriverBehaviorEventDtoPagedResponse>> {
  const rb = new RequestBuilder(rootUrl, getDriverBehaviorEvents.PATH, 'get');
  if (params) {
    rb.query('DriverId', params.DriverId, {});
    rb.query('TruckId', params.TruckId, {});
    rb.query('EventType', params.EventType, {});
    rb.query('IsReviewed', params.IsReviewed, {});
    rb.query('FromDate', params.FromDate, {});
    rb.query('ToDate', params.ToDate, {});
    rb.query('Search', params.Search, {});
    rb.query('OrderBy', params.OrderBy, {});
    rb.query('Page', params.Page, {});
    rb.query('PageSize', params.PageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DriverBehaviorEventDtoPagedResponse>;
    })
  );
}

getDriverBehaviorEvents.PATH = '/safety/behavior';
