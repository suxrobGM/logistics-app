/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { ProcessPublicPaymentRequest } from '../../models/process-public-payment-request';
import type { ProcessPublicPaymentResult } from '../../models/process-public-payment-result';

export interface ProcessPublicPayment$Params {
  tenantId: string;
  token: string;
      body?: ProcessPublicPaymentRequest
}

export function processPublicPayment(http: HttpClient, rootUrl: string, params: ProcessPublicPayment$Params, context?: HttpContext): Observable<StrictHttpResponse<ProcessPublicPaymentResult>> {
  const rb = new RequestBuilder(rootUrl, processPublicPayment.PATH, 'post');
  if (params) {
    rb.path('tenantId', params.tenantId, {});
    rb.path('token', params.token, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ProcessPublicPaymentResult>;
    })
  );
}

processPublicPayment.PATH = '/public/payments/{tenantId}/{token}/pay';
