/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { InvoiceLineItemDto } from '../../models/invoice-line-item-dto';
import type { UpdateLineItemRequest } from '../../models/update-line-item-request';

export interface UpdateLineItem$Params {
  invoiceId: string;
  lineItemId: string;
      body?: UpdateLineItemRequest
}

export function updateLineItem(http: HttpClient, rootUrl: string, params: UpdateLineItem$Params, context?: HttpContext): Observable<StrictHttpResponse<InvoiceLineItemDto>> {
  const rb = new RequestBuilder(rootUrl, updateLineItem.PATH, 'put');
  if (params) {
    rb.path('invoiceId', params.invoiceId, {});
    rb.path('lineItemId', params.lineItemId, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<InvoiceLineItemDto>;
    })
  );
}

updateLineItem.PATH = '/invoices/{invoiceId}/line-items/{lineItemId}';
