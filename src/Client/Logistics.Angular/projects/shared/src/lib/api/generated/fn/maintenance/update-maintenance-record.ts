/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { MaintenanceRecordDto } from '../../models/maintenance-record-dto';
import type { UpdateMaintenanceRecordCommand } from '../../models/update-maintenance-record-command';

export interface UpdateMaintenanceRecord$Params {
  id: string;
      body?: UpdateMaintenanceRecordCommand
}

export function updateMaintenanceRecord(http: HttpClient, rootUrl: string, params: UpdateMaintenanceRecord$Params, context?: HttpContext): Observable<StrictHttpResponse<MaintenanceRecordDto>> {
  const rb = new RequestBuilder(rootUrl, updateMaintenanceRecord.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<MaintenanceRecordDto>;
    })
  );
}

updateMaintenanceRecord.PATH = '/maintenance/records/{id}';
