/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface DeleteLineItem$Params {
  invoiceId: string;
  lineItemId: string;
}

export function deleteLineItem(http: HttpClient, rootUrl: string, params: DeleteLineItem$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, deleteLineItem.PATH, 'delete');
  if (params) {
    rb.path('invoiceId', params.invoiceId, {});
    rb.path('lineItemId', params.lineItemId, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

deleteLineItem.PATH = '/invoices/{invoiceId}/line-items/{lineItemId}';
