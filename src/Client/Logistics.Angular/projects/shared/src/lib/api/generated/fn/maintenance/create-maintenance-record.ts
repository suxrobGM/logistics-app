/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { CreateMaintenanceRecordCommand } from '../../models/create-maintenance-record-command';
import type { MaintenanceRecordDto } from '../../models/maintenance-record-dto';

export interface CreateMaintenanceRecord$Params {
      body?: CreateMaintenanceRecordCommand
}

export function createMaintenanceRecord(http: HttpClient, rootUrl: string, params?: CreateMaintenanceRecord$Params, context?: HttpContext): Observable<StrictHttpResponse<MaintenanceRecordDto>> {
  const rb = new RequestBuilder(rootUrl, createMaintenanceRecord.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<MaintenanceRecordDto>;
    })
  );
}

createMaintenanceRecord.PATH = '/maintenance/records';
