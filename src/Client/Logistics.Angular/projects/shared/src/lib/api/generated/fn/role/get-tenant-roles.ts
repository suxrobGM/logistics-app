/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { RoleDtoPagedResponse } from '../../models/role-dto-paged-response';

export interface GetTenantRoles$Params {
  Search?: string;
  OrderBy?: string;
  Page?: number;
  PageSize?: number;
}

export function getTenantRoles(http: HttpClient, rootUrl: string, params?: GetTenantRoles$Params, context?: HttpContext): Observable<StrictHttpResponse<RoleDtoPagedResponse>> {
  const rb = new RequestBuilder(rootUrl, getTenantRoles.PATH, 'get');
  if (params) {
    rb.query('Search', params.Search, {});
    rb.query('OrderBy', params.OrderBy, {});
    rb.query('Page', params.Page, {});
    rb.query('PageSize', params.PageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RoleDtoPagedResponse>;
    })
  );
}

getTenantRoles.PATH = '/roles/tenant';
