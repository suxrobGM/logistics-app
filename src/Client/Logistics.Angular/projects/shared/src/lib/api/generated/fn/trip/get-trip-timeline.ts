/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { TripTimelineDto } from '../../models/trip-timeline-dto';

export interface GetTripTimeline$Params {
  tripId: string;
}

export function getTripTimeline(http: HttpClient, rootUrl: string, params: GetTripTimeline$Params, context?: HttpContext): Observable<StrictHttpResponse<TripTimelineDto>> {
  const rb = new RequestBuilder(rootUrl, getTripTimeline.PATH, 'get');
  if (params) {
    rb.path('tripId', params.tripId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<TripTimelineDto>;
    })
  );
}

getTripTimeline.PATH = '/trips/{tripId}/timeline';
