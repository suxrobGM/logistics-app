/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { DvirReportDto } from '../../models/dvir-report-dto';

export interface GetDvirReportById$Params {
  id: string;
}

export function getDvirReportById(http: HttpClient, rootUrl: string, params: GetDvirReportById$Params, context?: HttpContext): Observable<StrictHttpResponse<DvirReportDto>> {
  const rb = new RequestBuilder(rootUrl, getDvirReportById.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DvirReportDto>;
    })
  );
}

getDvirReportById.PATH = '/dvir/{id}';
