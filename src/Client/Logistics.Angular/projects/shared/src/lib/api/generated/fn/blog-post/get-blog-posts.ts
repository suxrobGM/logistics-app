/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { BlogPostDtoPagedResponse } from '../../models/blog-post-dto-paged-response';
import type { BlogPostStatus } from '../../models/blog-post-status';

export interface GetBlogPosts$Params {
  Category?: string;
  Status?: BlogPostStatus;
  Search?: string;
  OrderBy?: string;
  Page?: number;
  PageSize?: number;
}

export function getBlogPosts(http: HttpClient, rootUrl: string, params?: GetBlogPosts$Params, context?: HttpContext): Observable<StrictHttpResponse<BlogPostDtoPagedResponse>> {
  const rb = new RequestBuilder(rootUrl, getBlogPosts.PATH, 'get');
  if (params) {
    rb.query('Category', params.Category, {});
    rb.query('Status', params.Status, {});
    rb.query('Search', params.Search, {});
    rb.query('OrderBy', params.OrderBy, {});
    rb.query('Page', params.Page, {});
    rb.query('PageSize', params.PageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<BlogPostDtoPagedResponse>;
    })
  );
}

getBlogPosts.PATH = '/blog/posts';
