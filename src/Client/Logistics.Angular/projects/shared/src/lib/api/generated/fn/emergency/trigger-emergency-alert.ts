/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { EmergencyAlertDto } from '../../models/emergency-alert-dto';
import type { TriggerEmergencyAlertCommand } from '../../models/trigger-emergency-alert-command';

export interface TriggerEmergencyAlert$Params {
      body?: TriggerEmergencyAlertCommand
}

export function triggerEmergencyAlert(http: HttpClient, rootUrl: string, params?: TriggerEmergencyAlert$Params, context?: HttpContext): Observable<StrictHttpResponse<EmergencyAlertDto>> {
  const rb = new RequestBuilder(rootUrl, triggerEmergencyAlert.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<EmergencyAlertDto>;
    })
  );
}

triggerEmergencyAlert.PATH = '/safety/emergency/trigger';
