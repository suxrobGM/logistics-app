/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { AddLineItemRequest } from '../../models/add-line-item-request';
import type { InvoiceLineItemDto } from '../../models/invoice-line-item-dto';

export interface AddLineItem$Params {
  id: string;
      body?: AddLineItemRequest
}

export function addLineItem(http: HttpClient, rootUrl: string, params: AddLineItem$Params, context?: HttpContext): Observable<StrictHttpResponse<InvoiceLineItemDto>> {
  const rb = new RequestBuilder(rootUrl, addLineItem.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<InvoiceLineItemDto>;
    })
  );
}

addLineItem.PATH = '/invoices/{id}/line-items';
