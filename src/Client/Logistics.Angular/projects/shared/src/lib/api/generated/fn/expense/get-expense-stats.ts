/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { ExpenseStatsDto } from '../../models/expense-stats-dto';

export interface GetExpenseStats$Params {
  FromDate?: string;
  ToDate?: string;
}

export function getExpenseStats(http: HttpClient, rootUrl: string, params?: GetExpenseStats$Params, context?: HttpContext): Observable<StrictHttpResponse<ExpenseStatsDto>> {
  const rb = new RequestBuilder(rootUrl, getExpenseStats.PATH, 'get');
  if (params) {
    rb.query('FromDate', params.FromDate, {});
    rb.query('ToDate', params.ToDate, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ExpenseStatsDto>;
    })
  );
}

getExpenseStats.PATH = '/expenses/stats';
