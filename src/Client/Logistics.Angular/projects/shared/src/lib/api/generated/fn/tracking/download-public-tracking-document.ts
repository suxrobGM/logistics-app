/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface DownloadPublicTrackingDocument$Params {
  tenantId: string;
  token: string;
  documentId: string;
}

export function downloadPublicTrackingDocument(http: HttpClient, rootUrl: string, params: DownloadPublicTrackingDocument$Params, context?: HttpContext): Observable<StrictHttpResponse<Blob>> {
  const rb = new RequestBuilder(rootUrl, downloadPublicTrackingDocument.PATH, 'get');
  if (params) {
    rb.path('tenantId', params.tenantId, {});
    rb.path('token', params.token, {});
    rb.path('documentId', params.documentId, {});
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Blob>;
    })
  );
}

downloadPublicTrackingDocument.PATH = '/tracking/{tenantId}/{token}/documents/{documentId}/download';
