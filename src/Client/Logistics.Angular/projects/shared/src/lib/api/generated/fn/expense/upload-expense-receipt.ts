/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { UploadReceiptResponse } from '../../models/upload-receipt-response';

export interface UploadExpenseReceipt$Params {
      body?: {
'File'?: Blob;
}
}

export function uploadExpenseReceipt(http: HttpClient, rootUrl: string, params?: UploadExpenseReceipt$Params, context?: HttpContext): Observable<StrictHttpResponse<UploadReceiptResponse>> {
  const rb = new RequestBuilder(rootUrl, uploadExpenseReceipt.PATH, 'post');
  if (params) {
    rb.body(params.body, 'multipart/form-data');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<UploadReceiptResponse>;
    })
  );
}

uploadExpenseReceipt.PATH = '/expenses/receipt';
