/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { BlogPostDtoPagedResponse } from '../../models/blog-post-dto-paged-response';

export interface GetPublishedBlogPosts$Params {
  Category?: string;
  Search?: string;
  OrderBy?: string;
  Page?: number;
  PageSize?: number;
}

export function getPublishedBlogPosts(http: HttpClient, rootUrl: string, params?: GetPublishedBlogPosts$Params, context?: HttpContext): Observable<StrictHttpResponse<BlogPostDtoPagedResponse>> {
  const rb = new RequestBuilder(rootUrl, getPublishedBlogPosts.PATH, 'get');
  if (params) {
    rb.query('Category', params.Category, {});
    rb.query('Search', params.Search, {});
    rb.query('OrderBy', params.OrderBy, {});
    rb.query('Page', params.Page, {});
    rb.query('PageSize', params.PageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<BlogPostDtoPagedResponse>;
    })
  );
}

getPublishedBlogPosts.PATH = '/blog/posts/published';
