/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { TimeEntryDtoPagedResponse } from '../../models/time-entry-dto-paged-response';

export interface GetTimeEntries$Params {
  EmployeeId?: string;
  StartDate?: string;
  EndDate?: string;
  Type?: string;
  IncludeLinkedToPayroll?: boolean;
  Search?: string;
  OrderBy?: string;
  Page?: number;
  PageSize?: number;
}

export function getTimeEntries(http: HttpClient, rootUrl: string, params?: GetTimeEntries$Params, context?: HttpContext): Observable<StrictHttpResponse<TimeEntryDtoPagedResponse>> {
  const rb = new RequestBuilder(rootUrl, getTimeEntries.PATH, 'get');
  if (params) {
    rb.query('EmployeeId', params.EmployeeId, {});
    rb.query('StartDate', params.StartDate, {});
    rb.query('EndDate', params.EndDate, {});
    rb.query('Type', params.Type, {});
    rb.query('IncludeLinkedToPayroll', params.IncludeLinkedToPayroll, {});
    rb.query('Search', params.Search, {});
    rb.query('OrderBy', params.OrderBy, {});
    rb.query('Page', params.Page, {});
    rb.query('PageSize', params.PageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<TimeEntryDtoPagedResponse>;
    })
  );
}

getTimeEntries.PATH = '/timeentries';
