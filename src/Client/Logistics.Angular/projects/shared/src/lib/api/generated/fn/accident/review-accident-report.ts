/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { AccidentReportDto } from '../../models/accident-report-dto';
import type { ReviewAccidentReportRequest } from '../../models/review-accident-report-request';

export interface ReviewAccidentReport$Params {
  id: string;
      body?: ReviewAccidentReportRequest
}

export function reviewAccidentReport(http: HttpClient, rootUrl: string, params: ReviewAccidentReport$Params, context?: HttpContext): Observable<StrictHttpResponse<AccidentReportDto>> {
  const rb = new RequestBuilder(rootUrl, reviewAccidentReport.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<AccidentReportDto>;
    })
  );
}

reviewAccidentReport.PATH = '/safety/accidents/{id}/review';
