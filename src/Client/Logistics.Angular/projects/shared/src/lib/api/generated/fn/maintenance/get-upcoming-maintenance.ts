/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { MaintenanceScheduleDto } from '../../models/maintenance-schedule-dto';

export interface GetUpcomingMaintenance$Params {
  daysAhead?: number;
  truckId?: string;
  includeOverdue?: boolean;
}

export function getUpcomingMaintenance(http: HttpClient, rootUrl: string, params?: GetUpcomingMaintenance$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<MaintenanceScheduleDto>>> {
  const rb = new RequestBuilder(rootUrl, getUpcomingMaintenance.PATH, 'get');
  if (params) {
    rb.query('daysAhead', params.daysAhead, {});
    rb.query('truckId', params.truckId, {});
    rb.query('includeOverdue', params.includeOverdue, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<MaintenanceScheduleDto>>;
    })
  );
}

getUpcomingMaintenance.PATH = '/maintenance/upcoming';
