/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { ContactSubmissionDtoPagedResponse } from '../../models/contact-submission-dto-paged-response';

export interface GetContactSubmissions$Params {
  Search?: string;
  OrderBy?: string;
  Page?: number;
  PageSize?: number;
}

export function getContactSubmissions(http: HttpClient, rootUrl: string, params?: GetContactSubmissions$Params, context?: HttpContext): Observable<StrictHttpResponse<ContactSubmissionDtoPagedResponse>> {
  const rb = new RequestBuilder(rootUrl, getContactSubmissions.PATH, 'get');
  if (params) {
    rb.query('Search', params.Search, {});
    rb.query('OrderBy', params.OrderBy, {});
    rb.query('Page', params.Page, {});
    rb.query('PageSize', params.PageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ContactSubmissionDtoPagedResponse>;
    })
  );
}

getContactSubmissions.PATH = '/contact-submissions';
