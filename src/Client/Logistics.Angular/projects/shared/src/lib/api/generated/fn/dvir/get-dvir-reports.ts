/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { DvirReportDtoPagedResponse } from '../../models/dvir-report-dto-paged-response';
import type { DvirStatus } from '../../models/dvir-status';
import type { DvirType } from '../../models/dvir-type';

export interface GetDvirReports$Params {
  TruckId?: string;
  DriverId?: string;
  Status?: DvirStatus;
  Type?: DvirType;
  FromDate?: string;
  ToDate?: string;
  Search?: string;
  OrderBy?: string;
  Page?: number;
  PageSize?: number;
}

export function getDvirReports(http: HttpClient, rootUrl: string, params?: GetDvirReports$Params, context?: HttpContext): Observable<StrictHttpResponse<DvirReportDtoPagedResponse>> {
  const rb = new RequestBuilder(rootUrl, getDvirReports.PATH, 'get');
  if (params) {
    rb.query('TruckId', params.TruckId, {});
    rb.query('DriverId', params.DriverId, {});
    rb.query('Status', params.Status, {});
    rb.query('Type', params.Type, {});
    rb.query('FromDate', params.FromDate, {});
    rb.query('ToDate', params.ToDate, {});
    rb.query('Search', params.Search, {});
    rb.query('OrderBy', params.OrderBy, {});
    rb.query('Page', params.Page, {});
    rb.query('PageSize', params.PageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DvirReportDtoPagedResponse>;
    })
  );
}

getDvirReports.PATH = '/dvir';
