/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { DispatchSessionDto } from '../../models/dispatch-session-dto';

export interface GetDispatchSessionById$Params {
  sessionId: string;
}

export function getDispatchSessionById(http: HttpClient, rootUrl: string, params: GetDispatchSessionById$Params, context?: HttpContext): Observable<StrictHttpResponse<DispatchSessionDto>> {
  const rb = new RequestBuilder(rootUrl, getDispatchSessionById.PATH, 'get');
  if (params) {
    rb.path('sessionId', params.sessionId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DispatchSessionDto>;
    })
  );
}

getDispatchSessionById.PATH = '/dispatch/agent/sessions/{sessionId}';
