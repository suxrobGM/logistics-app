/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { DispatchSessionDtoPagedResponse } from '../../models/dispatch-session-dto-paged-response';
import type { DispatchSessionStatus } from '../../models/dispatch-session-status';

export interface GetDispatchSessions$Params {
  Status?: DispatchSessionStatus;
  OrderBy?: string;
  Page?: number;
  PageSize?: number;
}

export function getDispatchSessions(http: HttpClient, rootUrl: string, params?: GetDispatchSessions$Params, context?: HttpContext): Observable<StrictHttpResponse<DispatchSessionDtoPagedResponse>> {
  const rb = new RequestBuilder(rootUrl, getDispatchSessions.PATH, 'get');
  if (params) {
    rb.query('Status', params.Status, {});
    rb.query('OrderBy', params.OrderBy, {});
    rb.query('Page', params.Page, {});
    rb.query('PageSize', params.PageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DispatchSessionDtoPagedResponse>;
    })
  );
}

getDispatchSessions.PATH = '/dispatch/agent/sessions';
