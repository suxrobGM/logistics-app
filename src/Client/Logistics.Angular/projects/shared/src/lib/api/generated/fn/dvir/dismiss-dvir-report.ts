/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { DismissDvirReportRequest } from '../../models/dismiss-dvir-report-request';
import type { DvirReportDto } from '../../models/dvir-report-dto';

export interface DismissDvirReport$Params {
  id: string;
      body?: DismissDvirReportRequest
}

export function dismissDvirReport(http: HttpClient, rootUrl: string, params: DismissDvirReport$Params, context?: HttpContext): Observable<StrictHttpResponse<DvirReportDto>> {
  const rb = new RequestBuilder(rootUrl, dismissDvirReport.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DvirReportDto>;
    })
  );
}

dismissDvirReport.PATH = '/dvir/{id}/dismiss';
