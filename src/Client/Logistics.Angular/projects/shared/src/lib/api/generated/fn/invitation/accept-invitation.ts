/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { AcceptInvitationCommand } from '../../models/accept-invitation-command';
import type { AcceptInvitationResult } from '../../models/accept-invitation-result';

export interface AcceptInvitation$Params {
      body?: AcceptInvitationCommand
}

export function acceptInvitation(http: HttpClient, rootUrl: string, params?: AcceptInvitation$Params, context?: HttpContext): Observable<StrictHttpResponse<AcceptInvitationResult>> {
  const rb = new RequestBuilder(rootUrl, acceptInvitation.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<AcceptInvitationResult>;
    })
  );
}

acceptInvitation.PATH = '/invitations/accept';
