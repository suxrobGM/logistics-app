/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { LoadBoardProviderType } from '../../models/load-board-provider-type';
import type { PostedTruckDto } from '../../models/posted-truck-dto';
import type { PostedTruckStatus } from '../../models/posted-truck-status';

export interface GetPostedTrucks$Params {
  ProviderType?: LoadBoardProviderType;
  Status?: PostedTruckStatus;
  TruckId?: string;
}

export function getPostedTrucks(http: HttpClient, rootUrl: string, params?: GetPostedTrucks$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<PostedTruckDto>>> {
  const rb = new RequestBuilder(rootUrl, getPostedTrucks.PATH, 'get');
  if (params) {
    rb.query('ProviderType', params.ProviderType, {});
    rb.query('Status', params.Status, {});
    rb.query('TruckId', params.TruckId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<PostedTruckDto>>;
    })
  );
}

getPostedTrucks.PATH = '/loadboard/trucks';
