/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { CreateEmergencyContactCommand } from '../../models/create-emergency-contact-command';
import type { EmergencyContactDto } from '../../models/emergency-contact-dto';

export interface CreateEmergencyContact$Params {
      body?: CreateEmergencyContactCommand
}

export function createEmergencyContact(http: HttpClient, rootUrl: string, params?: CreateEmergencyContact$Params, context?: HttpContext): Observable<StrictHttpResponse<EmergencyContactDto>> {
  const rb = new RequestBuilder(rootUrl, createEmergencyContact.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<EmergencyContactDto>;
    })
  );
}

createEmergencyContact.PATH = '/safety/emergency/contacts';
