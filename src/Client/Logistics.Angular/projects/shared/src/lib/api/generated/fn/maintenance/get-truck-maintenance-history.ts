/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { MaintenanceRecordDtoPagedResponse } from '../../models/maintenance-record-dto-paged-response';

export interface GetTruckMaintenanceHistory$Params {
  truckId: string;
  page?: number;
  pageSize?: number;
}

export function getTruckMaintenanceHistory(http: HttpClient, rootUrl: string, params: GetTruckMaintenanceHistory$Params, context?: HttpContext): Observable<StrictHttpResponse<MaintenanceRecordDtoPagedResponse>> {
  const rb = new RequestBuilder(rootUrl, getTruckMaintenanceHistory.PATH, 'get');
  if (params) {
    rb.path('truckId', params.truckId, {});
    rb.query('page', params.page, {});
    rb.query('pageSize', params.pageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<MaintenanceRecordDtoPagedResponse>;
    })
  );
}

getTruckMaintenanceHistory.PATH = '/maintenance/trucks/{truckId}/history';
