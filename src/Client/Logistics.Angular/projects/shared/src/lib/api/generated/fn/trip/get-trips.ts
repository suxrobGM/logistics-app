/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { TripDtoPagedResponse } from '../../models/trip-dto-paged-response';
import type { TripStatus } from '../../models/trip-status';

export interface GetTrips$Params {
  Name?: string;
  Status?: TripStatus;
  TruckNumber?: string;
  TruckId?: string;
  StartDate?: string;
  EndDate?: string;
  OnlyActiveTrips?: boolean;
  OrderBy?: string;
  Page?: number;
  PageSize?: number;
}

export function getTrips(http: HttpClient, rootUrl: string, params?: GetTrips$Params, context?: HttpContext): Observable<StrictHttpResponse<TripDtoPagedResponse>> {
  const rb = new RequestBuilder(rootUrl, getTrips.PATH, 'get');
  if (params) {
    rb.query('Name', params.Name, {});
    rb.query('Status', params.Status, {});
    rb.query('TruckNumber', params.TruckNumber, {});
    rb.query('TruckId', params.TruckId, {});
    rb.query('StartDate', params.StartDate, {});
    rb.query('EndDate', params.EndDate, {});
    rb.query('OnlyActiveTrips', params.OnlyActiveTrips, {});
    rb.query('OrderBy', params.OrderBy, {});
    rb.query('Page', params.Page, {});
    rb.query('PageSize', params.PageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<TripDtoPagedResponse>;
    })
  );
}

getTrips.PATH = '/trips';
