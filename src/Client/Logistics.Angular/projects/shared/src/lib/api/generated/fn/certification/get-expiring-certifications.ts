/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { DriverCertificationDto } from '../../models/driver-certification-dto';

export interface GetExpiringCertifications$Params {
  daysUntilExpiration?: number;
}

export function getExpiringCertifications(http: HttpClient, rootUrl: string, params?: GetExpiringCertifications$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<DriverCertificationDto>>> {
  const rb = new RequestBuilder(rootUrl, getExpiringCertifications.PATH, 'get');
  if (params) {
    rb.query('daysUntilExpiration', params.daysUntilExpiration, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<DriverCertificationDto>>;
    })
  );
}

getExpiringCertifications.PATH = '/certifications/expiring';
