/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { EmergencyContactDtoPagedResponse } from '../../models/emergency-contact-dto-paged-response';
import type { EmergencyContactType } from '../../models/emergency-contact-type';

export interface GetEmergencyContacts$Params {
  EmployeeId?: string;
  ContactType?: EmergencyContactType;
  IsActive?: boolean;
  Search?: string;
  OrderBy?: string;
  Page?: number;
  PageSize?: number;
}

export function getEmergencyContacts(http: HttpClient, rootUrl: string, params?: GetEmergencyContacts$Params, context?: HttpContext): Observable<StrictHttpResponse<EmergencyContactDtoPagedResponse>> {
  const rb = new RequestBuilder(rootUrl, getEmergencyContacts.PATH, 'get');
  if (params) {
    rb.query('EmployeeId', params.EmployeeId, {});
    rb.query('ContactType', params.ContactType, {});
    rb.query('IsActive', params.IsActive, {});
    rb.query('Search', params.Search, {});
    rb.query('OrderBy', params.OrderBy, {});
    rb.query('Page', params.Page, {});
    rb.query('PageSize', params.PageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<EmergencyContactDtoPagedResponse>;
    })
  );
}

getEmergencyContacts.PATH = '/safety/emergency/contacts';
