/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { CertificationStatus } from '../../models/certification-status';
import type { CertificationType } from '../../models/certification-type';
import type { DriverCertificationDtoPagedResponse } from '../../models/driver-certification-dto-paged-response';

export interface GetCertifications$Params {
  EmployeeId?: string;
  Type?: CertificationType;
  Status?: CertificationStatus;
  Search?: string;
  OrderBy?: string;
  Page?: number;
  PageSize?: number;
}

export function getCertifications(http: HttpClient, rootUrl: string, params?: GetCertifications$Params, context?: HttpContext): Observable<StrictHttpResponse<DriverCertificationDtoPagedResponse>> {
  const rb = new RequestBuilder(rootUrl, getCertifications.PATH, 'get');
  if (params) {
    rb.query('EmployeeId', params.EmployeeId, {});
    rb.query('Type', params.Type, {});
    rb.query('Status', params.Status, {});
    rb.query('Search', params.Search, {});
    rb.query('OrderBy', params.OrderBy, {});
    rb.query('Page', params.Page, {});
    rb.query('PageSize', params.PageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DriverCertificationDtoPagedResponse>;
    })
  );
}

getCertifications.PATH = '/certifications';
