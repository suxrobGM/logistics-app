/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { GuidResult } from '../../models/guid-result';
import type { RunDispatchAgentCommand } from '../../models/run-dispatch-agent-command';

export interface RunDispatchAgent$Params {
      body?: RunDispatchAgentCommand
}

export function runDispatchAgent(http: HttpClient, rootUrl: string, params?: RunDispatchAgent$Params, context?: HttpContext): Observable<StrictHttpResponse<GuidResult>> {
  const rb = new RequestBuilder(rootUrl, runDispatchAgent.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<GuidResult>;
    })
  );
}

runDispatchAgent.PATH = '/dispatch/agent/run';
