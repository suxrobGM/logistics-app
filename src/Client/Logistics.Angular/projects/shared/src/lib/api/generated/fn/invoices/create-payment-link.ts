/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { CreatePaymentLinkRequest } from '../../models/create-payment-link-request';
import type { PaymentLinkDto } from '../../models/payment-link-dto';

export interface CreatePaymentLink$Params {
  id: string;
      body?: CreatePaymentLinkRequest
}

export function createPaymentLink(http: HttpClient, rootUrl: string, params: CreatePaymentLink$Params, context?: HttpContext): Observable<StrictHttpResponse<PaymentLinkDto>> {
  const rb = new RequestBuilder(rootUrl, createPaymentLink.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PaymentLinkDto>;
    })
  );
}

createPaymentLink.PATH = '/invoices/{id}/payment-links';
