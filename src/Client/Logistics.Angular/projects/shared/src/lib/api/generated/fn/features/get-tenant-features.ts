/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { FeatureStatusDto } from '../../models/feature-status-dto';

export interface GetTenantFeatures$Params {
  tenantId: string;
}

export function getTenantFeatures(http: HttpClient, rootUrl: string, params: GetTenantFeatures$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<FeatureStatusDto>>> {
  const rb = new RequestBuilder(rootUrl, getTenantFeatures.PATH, 'get');
  if (params) {
    rb.path('tenantId', params.tenantId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<FeatureStatusDto>>;
    })
  );
}

getTenantFeatures.PATH = '/features/tenant/{tenantId}';
