/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { PublicTrackingDto } from '../../models/public-tracking-dto';

export interface GetPublicTracking$Params {
  tenantId: string;
  token: string;
}

export function getPublicTracking(http: HttpClient, rootUrl: string, params: GetPublicTracking$Params, context?: HttpContext): Observable<StrictHttpResponse<PublicTrackingDto>> {
  const rb = new RequestBuilder(rootUrl, getPublicTracking.PATH, 'get');
  if (params) {
    rb.path('tenantId', params.tenantId, {});
    rb.path('token', params.token, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PublicTrackingDto>;
    })
  );
}

getPublicTracking.PATH = '/tracking/{tenantId}/{token}';
