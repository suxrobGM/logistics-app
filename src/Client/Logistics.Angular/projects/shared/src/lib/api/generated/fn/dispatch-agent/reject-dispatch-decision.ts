/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { RejectDispatchDecisionCommand } from '../../models/reject-dispatch-decision-command';

export interface RejectDispatchDecision$Params {
  decisionId: string;
      body?: RejectDispatchDecisionCommand
}

export function rejectDispatchDecision(http: HttpClient, rootUrl: string, params: RejectDispatchDecision$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, rejectDispatchDecision.PATH, 'post');
  if (params) {
    rb.path('decisionId', params.decisionId, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

rejectDispatchDecision.PATH = '/dispatch/agent/decisions/{decisionId}/reject';
