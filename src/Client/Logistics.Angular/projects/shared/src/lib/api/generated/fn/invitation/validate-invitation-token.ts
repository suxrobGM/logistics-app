/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { InvitationValidationResult } from '../../models/invitation-validation-result';

export interface ValidateInvitationToken$Params {
  token: string;
}

export function validateInvitationToken(http: HttpClient, rootUrl: string, params: ValidateInvitationToken$Params, context?: HttpContext): Observable<StrictHttpResponse<InvitationValidationResult>> {
  const rb = new RequestBuilder(rootUrl, validateInvitationToken.PATH, 'get');
  if (params) {
    rb.path('token', params.token, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<InvitationValidationResult>;
    })
  );
}

validateInvitationToken.PATH = '/invitations/validate/{token}';
