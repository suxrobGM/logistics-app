/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { DriverBehaviorEventDto } from '../../models/driver-behavior-event-dto';
import type { ReviewDriverBehaviorEventCommand } from '../../models/review-driver-behavior-event-command';

export interface ReviewDriverBehaviorEvent$Params {
  id: string;
      body?: ReviewDriverBehaviorEventCommand
}

export function reviewDriverBehaviorEvent(http: HttpClient, rootUrl: string, params: ReviewDriverBehaviorEvent$Params, context?: HttpContext): Observable<StrictHttpResponse<DriverBehaviorEventDto>> {
  const rb = new RequestBuilder(rootUrl, reviewDriverBehaviorEvent.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DriverBehaviorEventDto>;
    })
  );
}

reviewDriverBehaviorEvent.PATH = '/safety/behavior/{id}/review';
