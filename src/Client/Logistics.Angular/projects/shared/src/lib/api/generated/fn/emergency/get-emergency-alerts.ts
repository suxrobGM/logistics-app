/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { EmergencyAlertDtoPagedResponse } from '../../models/emergency-alert-dto-paged-response';
import type { EmergencyAlertStatus } from '../../models/emergency-alert-status';
import type { EmergencyAlertType } from '../../models/emergency-alert-type';

export interface GetEmergencyAlerts$Params {
  DriverId?: string;
  Status?: EmergencyAlertStatus;
  Type?: EmergencyAlertType;
  FromDate?: string;
  ToDate?: string;
  Search?: string;
  OrderBy?: string;
  Page?: number;
  PageSize?: number;
}

export function getEmergencyAlerts(http: HttpClient, rootUrl: string, params?: GetEmergencyAlerts$Params, context?: HttpContext): Observable<StrictHttpResponse<EmergencyAlertDtoPagedResponse>> {
  const rb = new RequestBuilder(rootUrl, getEmergencyAlerts.PATH, 'get');
  if (params) {
    rb.query('DriverId', params.DriverId, {});
    rb.query('Status', params.Status, {});
    rb.query('Type', params.Type, {});
    rb.query('FromDate', params.FromDate, {});
    rb.query('ToDate', params.ToDate, {});
    rb.query('Search', params.Search, {});
    rb.query('OrderBy', params.OrderBy, {});
    rb.query('Page', params.Page, {});
    rb.query('PageSize', params.PageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<EmergencyAlertDtoPagedResponse>;
    })
  );
}

getEmergencyAlerts.PATH = '/safety/emergency/alerts';
