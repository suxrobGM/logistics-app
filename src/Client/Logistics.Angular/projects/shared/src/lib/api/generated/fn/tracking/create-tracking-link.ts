/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { CreateTrackingLinkCommand } from '../../models/create-tracking-link-command';
import type { TrackingLinkDto } from '../../models/tracking-link-dto';

export interface CreateTrackingLink$Params {
      body?: CreateTrackingLinkCommand
}

export function createTrackingLink(http: HttpClient, rootUrl: string, params?: CreateTrackingLink$Params, context?: HttpContext): Observable<StrictHttpResponse<TrackingLinkDto>> {
  const rb = new RequestBuilder(rootUrl, createTrackingLink.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<TrackingLinkDto>;
    })
  );
}

createTrackingLink.PATH = '/tracking';
