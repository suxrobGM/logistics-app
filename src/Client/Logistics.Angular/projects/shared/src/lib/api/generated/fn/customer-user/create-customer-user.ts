/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { CreateCustomerUserCommand } from '../../models/create-customer-user-command';
import type { CustomerUserDto } from '../../models/customer-user-dto';

export interface CreateCustomerUser$Params {
      body?: CreateCustomerUserCommand
}

export function createCustomerUser(http: HttpClient, rootUrl: string, params?: CreateCustomerUser$Params, context?: HttpContext): Observable<StrictHttpResponse<CustomerUserDto>> {
  const rb = new RequestBuilder(rootUrl, createCustomerUser.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CustomerUserDto>;
    })
  );
}

createCustomerUser.PATH = '/customer-users';
