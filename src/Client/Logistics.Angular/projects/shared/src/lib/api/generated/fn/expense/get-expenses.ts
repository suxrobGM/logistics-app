/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { ExpenseDtoPagedResponse } from '../../models/expense-dto-paged-response';
import type { ExpenseStatus } from '../../models/expense-status';
import type { ExpenseType } from '../../models/expense-type';

export interface GetExpenses$Params {
  Type?: ExpenseType;
  Status?: ExpenseStatus;
  TruckId?: string;
  FromDate?: string;
  ToDate?: string;
  Search?: string;
  OrderBy?: string;
  Page?: number;
  PageSize?: number;
}

export function getExpenses(http: HttpClient, rootUrl: string, params?: GetExpenses$Params, context?: HttpContext): Observable<StrictHttpResponse<ExpenseDtoPagedResponse>> {
  const rb = new RequestBuilder(rootUrl, getExpenses.PATH, 'get');
  if (params) {
    rb.query('Type', params.Type, {});
    rb.query('Status', params.Status, {});
    rb.query('TruckId', params.TruckId, {});
    rb.query('FromDate', params.FromDate, {});
    rb.query('ToDate', params.ToDate, {});
    rb.query('Search', params.Search, {});
    rb.query('OrderBy', params.OrderBy, {});
    rb.query('Page', params.Page, {});
    rb.query('PageSize', params.PageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ExpenseDtoPagedResponse>;
    })
  );
}

getExpenses.PATH = '/expenses';
