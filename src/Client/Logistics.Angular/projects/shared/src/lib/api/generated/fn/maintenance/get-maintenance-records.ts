/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { MaintenanceRecordDtoPagedResponse } from '../../models/maintenance-record-dto-paged-response';
import type { MaintenanceType } from '../../models/maintenance-type';

export interface GetMaintenanceRecords$Params {
  TruckId?: string;
  Type?: MaintenanceType;
  FromDate?: string;
  ToDate?: string;
  Search?: string;
  OrderBy?: string;
  Page?: number;
  PageSize?: number;
}

export function getMaintenanceRecords(http: HttpClient, rootUrl: string, params?: GetMaintenanceRecords$Params, context?: HttpContext): Observable<StrictHttpResponse<MaintenanceRecordDtoPagedResponse>> {
  const rb = new RequestBuilder(rootUrl, getMaintenanceRecords.PATH, 'get');
  if (params) {
    rb.query('TruckId', params.TruckId, {});
    rb.query('Type', params.Type, {});
    rb.query('FromDate', params.FromDate, {});
    rb.query('ToDate', params.ToDate, {});
    rb.query('Search', params.Search, {});
    rb.query('OrderBy', params.OrderBy, {});
    rb.query('Page', params.Page, {});
    rb.query('PageSize', params.PageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<MaintenanceRecordDtoPagedResponse>;
    })
  );
}

getMaintenanceRecords.PATH = '/maintenance/records';
