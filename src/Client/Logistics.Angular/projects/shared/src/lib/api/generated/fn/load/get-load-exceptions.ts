/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { LoadExceptionDto } from '../../models/load-exception-dto';

export interface GetLoadExceptions$Params {
  id: string;
}

export function getLoadExceptions(http: HttpClient, rootUrl: string, params: GetLoadExceptions$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<LoadExceptionDto>>> {
  const rb = new RequestBuilder(rootUrl, getLoadExceptions.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<LoadExceptionDto>>;
    })
  );
}

getLoadExceptions.PATH = '/loads/{id}/exceptions';
