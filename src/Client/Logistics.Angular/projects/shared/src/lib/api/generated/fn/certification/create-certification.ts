/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { CreateDriverCertificationCommand } from '../../models/create-driver-certification-command';
import type { DriverCertificationDto } from '../../models/driver-certification-dto';

export interface CreateCertification$Params {
      body?: CreateDriverCertificationCommand
}

export function createCertification(http: HttpClient, rootUrl: string, params?: CreateCertification$Params, context?: HttpContext): Observable<StrictHttpResponse<DriverCertificationDto>> {
  const rb = new RequestBuilder(rootUrl, createCertification.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DriverCertificationDto>;
    })
  );
}

createCertification.PATH = '/certifications';
