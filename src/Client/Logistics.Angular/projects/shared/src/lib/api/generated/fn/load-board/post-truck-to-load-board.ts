/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { PostTruckResultDto } from '../../models/post-truck-result-dto';
import type { PostTruckToLoadBoardCommand } from '../../models/post-truck-to-load-board-command';

export interface PostTruckToLoadBoard$Params {
      body?: PostTruckToLoadBoardCommand
}

export function postTruckToLoadBoard(http: HttpClient, rootUrl: string, params?: PostTruckToLoadBoard$Params, context?: HttpContext): Observable<StrictHttpResponse<PostTruckResultDto>> {
  const rb = new RequestBuilder(rootUrl, postTruckToLoadBoard.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PostTruckResultDto>;
    })
  );
}

postTruckToLoadBoard.PATH = '/loadboard/trucks';
