/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { BlogPostDto } from '../../models/blog-post-dto';

export interface GetBlogPostById$Params {
  id: string;
}

export function getBlogPostById(http: HttpClient, rootUrl: string, params: GetBlogPostById$Params, context?: HttpContext): Observable<StrictHttpResponse<BlogPostDto>> {
  const rb = new RequestBuilder(rootUrl, getBlogPostById.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<BlogPostDto>;
    })
  );
}

getBlogPostById.PATH = '/blog/posts/{id}';
