/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { DocumentDto } from '../../models/document-dto';

export interface GetPublicTrackingDocuments$Params {
  tenantId: string;
  token: string;
}

export function getPublicTrackingDocuments(http: HttpClient, rootUrl: string, params: GetPublicTrackingDocuments$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<DocumentDto>>> {
  const rb = new RequestBuilder(rootUrl, getPublicTrackingDocuments.PATH, 'get');
  if (params) {
    rb.path('tenantId', params.tenantId, {});
    rb.path('token', params.token, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<DocumentDto>>;
    })
  );
}

getPublicTrackingDocuments.PATH = '/tracking/{tenantId}/{token}/documents';
