/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { UpdateTenantFeaturesAdminCommand } from '../../models/update-tenant-features-admin-command';

export interface UpdateTenantFeatures$Params {
  tenantId: string;
      body?: UpdateTenantFeaturesAdminCommand
}

export function updateTenantFeatures(http: HttpClient, rootUrl: string, params: UpdateTenantFeatures$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, updateTenantFeatures.PATH, 'put');
  if (params) {
    rb.path('tenantId', params.tenantId, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

updateTenantFeatures.PATH = '/features/tenant/{tenantId}';
