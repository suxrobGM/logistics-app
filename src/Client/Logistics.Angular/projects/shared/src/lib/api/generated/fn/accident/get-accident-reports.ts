/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { AccidentReportDtoPagedResponse } from '../../models/accident-report-dto-paged-response';
import type { AccidentReportStatus } from '../../models/accident-report-status';
import type { AccidentSeverity } from '../../models/accident-severity';

export interface GetAccidentReports$Params {
  TruckId?: string;
  DriverId?: string;
  Status?: AccidentReportStatus;
  Severity?: AccidentSeverity;
  FromDate?: string;
  ToDate?: string;
  Search?: string;
  OrderBy?: string;
  Page?: number;
  PageSize?: number;
}

export function getAccidentReports(http: HttpClient, rootUrl: string, params?: GetAccidentReports$Params, context?: HttpContext): Observable<StrictHttpResponse<AccidentReportDtoPagedResponse>> {
  const rb = new RequestBuilder(rootUrl, getAccidentReports.PATH, 'get');
  if (params) {
    rb.query('TruckId', params.TruckId, {});
    rb.query('DriverId', params.DriverId, {});
    rb.query('Status', params.Status, {});
    rb.query('Severity', params.Severity, {});
    rb.query('FromDate', params.FromDate, {});
    rb.query('ToDate', params.ToDate, {});
    rb.query('Search', params.Search, {});
    rb.query('OrderBy', params.OrderBy, {});
    rb.query('Page', params.Page, {});
    rb.query('PageSize', params.PageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<AccidentReportDtoPagedResponse>;
    })
  );
}

getAccidentReports.PATH = '/safety/accidents';
