/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { DashboardLinkDto } from '../../models/dashboard-link-dto';

export interface GetDashboardLink$Params {
}

export function getDashboardLink(http: HttpClient, rootUrl: string, params?: GetDashboardLink$Params, context?: HttpContext): Observable<StrictHttpResponse<DashboardLinkDto>> {
  const rb = new RequestBuilder(rootUrl, getDashboardLink.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DashboardLinkDto>;
    })
  );
}

getDashboardLink.PATH = '/stripe/connect/dashboard-link';
