/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { AccidentReportDto } from '../../models/accident-report-dto';
import type { UpdateAccidentReportCommand } from '../../models/update-accident-report-command';

export interface UpdateAccidentReport$Params {
  id: string;
      body?: UpdateAccidentReportCommand
}

export function updateAccidentReport(http: HttpClient, rootUrl: string, params: UpdateAccidentReport$Params, context?: HttpContext): Observable<StrictHttpResponse<AccidentReportDto>> {
  const rb = new RequestBuilder(rootUrl, updateAccidentReport.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<AccidentReportDto>;
    })
  );
}

updateAccidentReport.PATH = '/safety/accidents/{id}';
