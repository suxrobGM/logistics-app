/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { InvitationDtoPagedResponse } from '../../models/invitation-dto-paged-response';
import type { InvitationStatus } from '../../models/invitation-status';
import type { InvitationType } from '../../models/invitation-type';

export interface GetInvitations$Params {
  Status?: InvitationStatus;
  Type?: InvitationType;
  Search?: string;
  OrderBy?: string;
  Page?: number;
  PageSize?: number;
}

export function getInvitations(http: HttpClient, rootUrl: string, params?: GetInvitations$Params, context?: HttpContext): Observable<StrictHttpResponse<InvitationDtoPagedResponse>> {
  const rb = new RequestBuilder(rootUrl, getInvitations.PATH, 'get');
  if (params) {
    rb.query('Status', params.Status, {});
    rb.query('Type', params.Type, {});
    rb.query('Search', params.Search, {});
    rb.query('OrderBy', params.OrderBy, {});
    rb.query('Page', params.Page, {});
    rb.query('PageSize', params.PageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<InvitationDtoPagedResponse>;
    })
  );
}

getInvitations.PATH = '/invitations';
