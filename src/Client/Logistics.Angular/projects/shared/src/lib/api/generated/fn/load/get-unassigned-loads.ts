/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { LoadDtoPagedResponse } from '../../models/load-dto-paged-response';

export interface GetUnassignedLoads$Params {
  OrderBy?: string;
  Page?: number;
  PageSize?: number;
}

export function getUnassignedLoads(http: HttpClient, rootUrl: string, params?: GetUnassignedLoads$Params, context?: HttpContext): Observable<StrictHttpResponse<LoadDtoPagedResponse>> {
  const rb = new RequestBuilder(rootUrl, getUnassignedLoads.PATH, 'get');
  if (params) {
    rb.query('OrderBy', params.OrderBy, {});
    rb.query('Page', params.Page, {});
    rb.query('PageSize', params.PageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<LoadDtoPagedResponse>;
    })
  );
}

getUnassignedLoads.PATH = '/loads/unassigned';
