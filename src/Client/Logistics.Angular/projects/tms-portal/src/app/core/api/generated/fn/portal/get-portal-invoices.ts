/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { PortalInvoiceDtoPagedResponse } from '../../models/portal-invoice-dto-paged-response';

export interface GetPortalInvoices$Params {
  Search?: string;
  Page?: number;
  PageSize?: number;
  OrderBy?: string;
  StartDate?: string;
  EndDate?: string;
}

export function getPortalInvoices(http: HttpClient, rootUrl: string, params?: GetPortalInvoices$Params, context?: HttpContext): Observable<StrictHttpResponse<PortalInvoiceDtoPagedResponse>> {
  const rb = new RequestBuilder(rootUrl, getPortalInvoices.PATH, 'get');
  if (params) {
    rb.query('Search', params.Search, {});
    rb.query('Page', params.Page, {});
    rb.query('PageSize', params.PageSize, {});
    rb.query('OrderBy', params.OrderBy, {});
    rb.query('StartDate', params.StartDate, {});
    rb.query('EndDate', params.EndDate, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PortalInvoiceDtoPagedResponse>;
    })
  );
}

getPortalInvoices.PATH = '/portal/invoices';
