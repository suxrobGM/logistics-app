/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { CustomerUserDto } from '../../models/customer-user-dto';

export interface GetCustomerUsersByCustomer$Params {
  customerId: string;
}

export function getCustomerUsersByCustomer(http: HttpClient, rootUrl: string, params: GetCustomerUsersByCustomer$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<CustomerUserDto>>> {
  const rb = new RequestBuilder(rootUrl, getCustomerUsersByCustomer.PATH, 'get');
  if (params) {
    rb.path('customerId', params.customerId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<CustomerUserDto>>;
    })
  );
}

getCustomerUsersByCustomer.PATH = '/customer-users/by-customer/{customerId}';
