/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { PayrollDto } from '../../models/payroll-dto';

export interface PreviewPayrollInvoice$Params {
  EmployeeId?: string;
  PeriodStart?: string;
  PeriodEnd?: string;
}

export function previewPayrollInvoice(http: HttpClient, rootUrl: string, params?: PreviewPayrollInvoice$Params, context?: HttpContext): Observable<StrictHttpResponse<PayrollDto>> {
  const rb = new RequestBuilder(rootUrl, previewPayrollInvoice.PATH, 'get');
  if (params) {
    rb.query('EmployeeId', params.EmployeeId, {});
    rb.query('PeriodStart', params.PeriodStart, {});
    rb.query('PeriodEnd', params.PeriodEnd, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PayrollDto>;
    })
  );
}

previewPayrollInvoice.PATH = '/invoices/payrolls/preview';
