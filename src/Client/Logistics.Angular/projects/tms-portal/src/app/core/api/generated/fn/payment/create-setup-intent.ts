/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { SetupIntentDto } from '../../models/setup-intent-dto';

export interface CreateSetupIntent$Params {
}

export function createSetupIntent(http: HttpClient, rootUrl: string, params?: CreateSetupIntent$Params, context?: HttpContext): Observable<StrictHttpResponse<SetupIntentDto>> {
  const rb = new RequestBuilder(rootUrl, createSetupIntent.PATH, 'post');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SetupIntentDto>;
    })
  );
}

createSetupIntent.PATH = '/payments/methods/setup-intent';
