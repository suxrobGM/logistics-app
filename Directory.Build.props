<Project>

  <PropertyGroup>
    <Version>1.0.0</Version>
    <!-- Keeps InformationalVersion free of the "+sha" suffix so the X-LogisticsX-Version header stays clean. -->
    <IncludeSourceRevisionInInformationalVersion>false</IncludeSourceRevisionInInformationalVersion>
  </PropertyGroup>

</Project>
