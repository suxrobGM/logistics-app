---
name: add-dispatch-tool
description: Add a new tool to the AI dispatch agent. Use when the user wants to give the agent a new capability (e.g., "add a tool that returns load board listings older than 24h"). Walks through the four files that must change and the silently load-bearing WriteTools step.
---

# add-dispatch-tool

This is a Codex discovery shim. The authoritative skill lives at:

@../../../.claude/skills/add-dispatch-tool/SKILL.md

When using this skill, read the authoritative file above and treat .claude/skills/add-dispatch-tool/ as the skill root for resolving relative paths, scripts, references, and assets.
